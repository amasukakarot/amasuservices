server:
  port: 30001
spring:
  application:
    name: fraud-service
  datasource:
    url: jdbc:postgresql://postgres:5432/postgres
    username: 'amasu'
    password: 'password'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

